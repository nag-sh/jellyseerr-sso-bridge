apiVersion: v1
kind: Service
metadata:
  name: {{ include "jellyseerr-sso-bridge.fullname" . }}
  labels:
    {{- include "jellyseerr-sso-bridge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jellyseerr-sso-bridge.selectorLabels" . | nindent 4 }}

